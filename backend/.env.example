# Optional — these have sensible defaults. LLM provider settings live in the DB at runtime, not here.

# Where SQLite, FAISS indices, uploaded docs, screenshots, and reports live (relative to backend/)
QAI_DATA_DIR=./data

# Logging level for the backend
QAI_LOG_LEVEL=INFO
